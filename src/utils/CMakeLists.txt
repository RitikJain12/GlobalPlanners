cmake_minimum_required(VERSION 3.5)
project(utils)

add_library(${PROJECT_NAME} src/node.cpp src/point.cpp)

target_include_directories(${PROJECT_NAME}
    PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include
)
